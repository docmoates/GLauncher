{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && if [ -n \"$(git status --porcelain)\" ]; then git add -A && git commit -q -m \"Auto-commit: sync Claude Code edits\" && git push -q -u origin \"$(git rev-parse --abbrev-ref HEAD)\" && echo '{\"systemMessage\": \"Auto-committed and pushed session edits\"}'; fi",
            "timeout": 120,
            "statusMessage": "Committing and pushing edits..."
          }
        ]
      }
    ]
  }
}
