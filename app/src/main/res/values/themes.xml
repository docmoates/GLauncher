<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Launcher theme: draws the system wallpaper behind the app and makes the
      window fully transparent + edge-to-edge, so the home screen feels like the
      Pixel Launcher (UI floating over the live wallpaper).
    -->
    <style name="Theme.GLauncher" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
